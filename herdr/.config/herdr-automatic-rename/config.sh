ICONS_ENABLED=1
ICON_STYLE=name_and_icon
AUTO_INDEX_WORKSPACES=0
# Leave agent rows alone: the plugin renames the agent itself, not just the tab,
# so a focus change on a sibling pane rewrites the agent's session name.
AUTO_INDEX_AGENTS=0
