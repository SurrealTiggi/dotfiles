---
# - doppler config for secrets
# https://community.doppler.com/t/ansible-shell-command-modules-with-doppler/267
# - fetch and configure ssh key
# - fetch and configure gpg keys
# - fetch and configure .env.secret accordingly
#
# Template:
# export <SECRET_FROM_DOPPLER>={{ lookup('env', 'SECRET_FROM_DOPPLER') }}

# Not wired up yet: the import in playbook.yml is commented out and
# configure_secrets is false. A task needs a module before it can run.
